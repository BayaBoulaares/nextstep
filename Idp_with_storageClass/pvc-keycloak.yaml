apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: keycloak-nfs-pvc
  namespace: tenant-baya
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-storage
  resources:
    requests:
      storage: 5Gi
