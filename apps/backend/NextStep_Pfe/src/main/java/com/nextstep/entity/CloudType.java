package com.nextstep.entity;


public enum CloudType {
    PRIVÉ,   // Cloud Privé  — infrastructure dédiée isolée
    PUBLIC,    // Cloud Public — infrastructure mutualisée pay-as-you-go
    HYBRIDE     // Cloud Hybride — connecte privé et public
}